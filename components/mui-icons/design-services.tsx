'use client'

import React from 'react'
import MuiDesignServicesIcon from '@mui/icons-material/DesignServices'

type TProps = React.ComponentProps<typeof MuiDesignServicesIcon>
export default function DesignServicesIcon(props: TProps): React.ReactElement {
  return <MuiDesignServicesIcon {...props} />
}