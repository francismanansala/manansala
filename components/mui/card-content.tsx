'use client'

import React from 'react'
import MuiCardContent, { CardContentProps } from '@mui/material/CardContent'

export default function CardContent(props: CardContentProps): React.ReactElement {
  return <MuiCardContent {...props} />
}